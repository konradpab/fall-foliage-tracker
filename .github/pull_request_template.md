## What this changes

<!-- One or two sentences. -->

## Checks

- [ ] `python -m pytest` passes
- [ ] `npm test` passes
- [ ] `npm run build && npm run size` passes
- [ ] If a data source was added, its failure path degrades to a status chip
- [ ] If a model constant changed, the PR says what it was fitted against and
      `README.md` is updated
